# Project Instructions

## Training & Eval Runs
- **ALWAYS** show the user the exact command and parameters for every training run and eval run **before** executing.
- If multiple tasks must run in sequence (e.g., train then eval), present **all** commands upfront.
- **Do not execute** until the user explicitly approves.
