"""Visualization package for sycophancy eval results.

Pipeline: raw_logs → MetricFrame (long-form) → transforms → bar_plot(theme).
Designed for parity with the legacy publication path in `visualize_results.py`.
"""
